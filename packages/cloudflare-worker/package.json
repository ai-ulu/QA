{
  "name": "@autoqa/mcp-server-cloudflare",
  "version": "2.1.0",
  "description": "AutoQA MCP Server with Web Audit - Change-aware Playwright maintenance copilot + Web site auditing",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev ./dist --d1=DB",
    "build": "esbuild src/index.ts --bundle --outdir=dist --format=esm --target=esnext --sourcemap",
    "deploy": "pnpm run build && wrangler pages deploy dist --project-name=autoqa-mcp",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.99.0"
  }
}
