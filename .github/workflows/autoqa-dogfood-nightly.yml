name: AutoQA Dogfood Nightly

on:
  schedule:
    - cron: '15 1 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dogfood-nightly:
    runs-on: ubuntu-latest

    steps:
      - name: Smoke
        run: echo "AutoQA Dogfood Nightly started"
